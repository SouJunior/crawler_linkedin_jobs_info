# upstream python_flask_app {
#     server app_crawler_linkedin_jobs_server:5000;
# }

# server {
#     listen 80;

#     location / {
#         proxy_pass http://python_flask_app;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header Host $host;
#         proxy_redirect off;
#     }
# }

# server {
#     listen 80;
#     index index.py;
#     root /var/www/public;

#     location ~ \.py$ {
#         add_header 'Access-Control-Allow-Origin' '*' always;

#         try_files $uri =404;
#         fastcgi_split_path_info ^(.+\.py)(/.+)$;
#         fastcgi_pass app_crawler_linkedin_jobs_server:5000;
#         fastcgi_index index.py;
#         include fastcgi_params;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_param PATH_INFO $fastcgi_path_info;
#         fastcgi_read_timeout 2h;
#     }

#     location / {
#         try_files $uri $uri/ /index.py?$query_string;
#         gzip_static on;

#         if ($request_method = 'OPTIONS') {
#             add_header 'Access-Control-Allow-Origin' '*';
#             add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, PATCH, DELETE, OPTIONS';
#             add_header 'Access-Control-Allow-Headers' 'Authorization,DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range';

#             return 204;
#         }
#     }

#     error_log  /var/log/nginx/error.log;
#     access_log /var/log/nginx/access.log;
# }

server {
    # listen 80 app_crawler_linkedin_jobs_server:5000;

    listen 80;

    index index.html index.php;

    # server_name docker.localhost;

    # location / {
    #     add_header 'Access-Control-Allow-Origin' '*' always;

    #     try_files $uri =404;

    #     uwsgi_pass app_crawler_linkedin_jobs_server:5000;
    #     include /etc/nginx/uwsgi_params;

    #     uwsgi_param Host $host;
    #     uwsgi_param X-Real-IP $remote_addr;
    #     uwsgi_param X-Forwarded-For $proxy_add_x_forwarded_for;
    #     uwsgi_param X-Forwarded-Proto $http_x_forwarded_proto;
    # }

    error_log /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;

    root /www;
}
